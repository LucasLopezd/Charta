import { PaginacionIngresoPipe } from './paginacion-ingreso.pipe';

describe('PaginacionIngresoPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginacionIngresoPipe();
    expect(pipe).toBeTruthy();
  });
});
