import { RecargaDirective } from './recarga.directive';

describe('RecargaDirective', () => {
  it('should create an instance', () => {
    const directive = new RecargaDirective();
    expect(directive).toBeTruthy();
  });
});
